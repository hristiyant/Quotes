<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hrisko.quotes">

    <application
        android:name=".AndroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity
            android:name=".views.QuotesList.QuotesListActivity"
            android:label="@string/title_activity_quotes_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".views.QuoteCreate.QuoteCreateActivity" />
        <activity android:name=".views.Details.QuoteDetailsActivity"></activity>
    </application>

</manifest>